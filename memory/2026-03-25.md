# Daily Memory — 2026-03-25

## Session: moments-game 3D Art Pass + Scene Build
**Time:** ~01:30–02:45 AM (Asia/Shanghai)

### What we built (this turn batch)

#### New Scenes written
- `Assets/Scenes/MiniGames/PolarPush.unity` — arctic arena, aurora+snowfall VFX, 8 spawn points, IcePlatformManager wired, 55° FOV cam at (0,18,-22)
- `Assets/Scenes/Attract.unity` — TV attract loop, hero color cycling title, QR panel, scan prompt blink
- `Assets/Scenes/Results.unity` — staggered card reveal, VerticalLayoutGroup, 8-second display
- `Assets/Scenes/Podium.unity` — 3-pedestal setup (Gold/Silver/Bronze heights), confetti burst, 1st-place spotlight, 55° cam

#### New Shaders (4 URP HLSL shaders)
- `Assets/Shaders/IceSurface.shader` — SSS approx, voronoi crack pattern, refraction, rim fresnel, crack decal, transparent
- `Assets/Shaders/CharacterLit.shader` — toy-style wrap lighting, player-color rim via MaterialPropertyBlock, win emission pulse
- `Assets/Shaders/OceanWater.shader` — Gerstner vertex waves, dual scrolling normals, foam at crests, depth-fog color blend
- `Assets/Shaders/TilePaint.shader` — paint lerp via BlendFactor, splat mask reveal, grid lines, emission edge pulse

#### New Scripts
- `Scripts/Gameplay/CharacterVisuals.cs` — MaterialPropertyBlock rim tint, dash trail color, win pulse coroutine, hit flash, elimination grey-out
- `Scripts/Core/VFXManager.cs` — object-pooled VFX (HitSpark, DashTrail, ScoreBurst, Confetti, Explosion, PaintSplat, IceCrack, ElimFlash, JoinPing); Play() with auto-return
- `Scripts/UI/AttractSceneController.cs` — title color cycling (8 hero colors), scan prompt blink, first-player-join → lobby transition

#### ScriptableObject Assets (YAML)
- 8x `CharacterDefinition` assets in `Assets/ScriptableObjects/Characters/` — all 8 heroes with correct hex, emoji, colors
- 7x `MiniGameDefinition` assets in `Assets/ScriptableObjects/MiniGames/` — all 7 games with scoring mode, duration, layout, sceneAddress

#### Materials (YAML)
- `Arenas/IceTile_Intact.mat`, `IceTile_Cracked.mat` — IceSurface shader
- `Arenas/ColorTile_Default.mat` — TilePaint shader
- 8x `Characters/Char_[Hero].mat` — CharacterLit shader, per-hero primary color
- 3x `UI/Podium_Gold/Silver/Bronze.mat` — metallic URP Lit

### Project state at end of turn
- Git last commit before this turn: `ab99feb`
- All new files added but NOT yet committed (commit pending)
- Unity project at: `moments-game/UnityProject/`
- Total scenes now: Bootstrap, Lobby, Attract, Results, Podium, MiniGames/PolarPush (6 of 10 planned)
- Missing scenes: ColorClash.unity, TankBattle.unity, WaveRider.unity, BumperBlitz.unity, BlinkShot.unity, GravityGrab.unity

### Key technical notes
- MaterialPropertyBlock pattern confirmed: `_RimColor`, `_EmissionColor`, `_EmissionStrength` property IDs cached as static
- VFXManager uses `DontDestroyOnLoad` — must live in Bootstrap scene
- CharacterVisuals.OnEliminated() disables Collider — important for Polar Push pile-ups
- AttractSceneController async-awaits MiniGameLoader.LoadLobby() — needs null check in production
- OceanWater shader uses 3-wave Gerstner sum at vertex stage (not tessellation) — Wave Rider ready
- TilePaint shader BlendFactor driven by script each frame — no MaterialPropertyBlock needed (single material per tile instance)

### What's still needed
1. Remaining 6 mini-game scenes (ColorClash, TankBattle, WaveRider, BumperBlitz, BlinkShot, GravityGrab)
2. `WaveRiderGame.cs`, `BumperBlitzGame.cs`, `BlinkShotGame.cs`, `GravityGrabGame.cs`
3. `ColorClashGame.cs` tile painting loop + `TankBattleGame.cs` projectile system (both exist as stubs in src/)
4. Prefab YAML files (character rigs, arena pieces, UI cards)
5. Build settings YAML (`ProjectSettings/EditorBuildSettings.asset`) with all scenes registered
6. Git commit of all new assets

---

## Session: Full Systems + Correctness Pass + 3D Art Pass
**Time:** ~03:00–05:54 AM (Asia/Shanghai)
**Commits:** `7211831`, `16a7ebe`, `847a85a`

### Commits summary
- `7211831` — All 7 mini-games complete + 6 missing scenes + EditorBuildSettings
- `16a7ebe` — Full correctness pass: compile errors fixed, systems wired end-to-end
- `847a85a` — Full 3D art pass: procedural meshes, VFX, materials, shaders, per-arena post-FX

### New gameplay scripts (4 full implementations)
- `WaveRiderGame.cs` — Gerstner wave surfing, tilt-to-balance wipeout, air tricks, crest 4× multiplier
- `BumperBlitzGame.cs` — bumper car physics, boost pads, 3 rotating hazard bumpers, per-hit scoring
- `BlinkShotGame.cs` — teleport raycast blink-to-target shooter, 3-hit elimination, cyber rooftop
- `GravityGrabGame.cs` — zero-G orb collection, 3 tiers (White/Blue/Gold), 180s timer, gravity flip mechanic

### All 6 missing scenes created
- `ColorClash.unity`, `TankBattle.unity`, `WaveRider.unity`, `BumperBlitz.unity`, `BlinkShot.unity`, `GravityGrab.unity`
- `EditorBuildSettings.asset` — all 12 scenes registered (indices 0–11)

### Bugs fixed (16a7ebe)
- `PlayerData.cs` — added `sessionScore`, `currentRoundScore`, `placement`, `ResetRoundState()`
- `SessionStateManager.cs` — added `LobbyState` enum alias + `ChangeState(LobbyState)` overload
- `ControllerGateway.cs` — added `SendHapticToPlayer`, `SendToPlayer`, `BroadcastCountdown`, `BroadcastUICommand`; fixed join message type `server_hello` → `welcome`
- `PodiumSceneController.cs` — replaced `System.Text.Json` (unavailable in Unity) with `JsonUtility`
- `AudioManager.cs` — added `OnCountdown()` method
- `ResultsSceneController.cs` — fixed async coroutine to be `async` for `Awaitable`
- `TankBattleGame.cs` — fixed alive-tank count logic (was inverted)
- `GravityGrabGame.cs` — renamed `IntListExtension.Sum()` to avoid LINQ ambiguity
- Added `UnityMainThreadDispatcher.cs` and `MainThreadFlusher.cs` (MonoBehaviour driver)
- Added `AddressableAssetSettings.asset`
- Added arena prefabs: `ColorTile.prefab`, `Cannonball.prefab`, `ShotPrefab.prefab`, `VFX_HitSpark.prefab`
- Updated phone controller `phone-controller.html` — full production rewrite with all 7 game layouts

### 3D Art pass (847a85a)
- `ProceduralMeshLibrary.cs` — 7 mesh generators: Hexagon, RoundedBox, SuperEllipsoidCapsule, Torus, OceanPlane (UInt32, 80×80), BoostArrow, GravityOrb
- `ArtBootstrap.cs` — swaps placeholder meshes at scene Awake for all arena + character objects
- `CharacterArtBuilder.cs` — per-hero geometry for all 8 heroes assembled from shared primitives; MaterialPropertyBlock only (zero GC)
- `VFXBuilder.cs` — 9 fully-configured particle systems built procedurally at runtime; VFXManager auto-fallbacks if no prefab assigned
- `ParticleAdditive.shader` — URP additive, per-particle color, depth softness, fog-compatible
- All 8 character materials fully tuned with per-hero PBR values (gloss, wrap, rim power, emission)
- 11 arena materials for TankBattle, WaveRider, BumperBlitz, BlinkShot, GravityGrab
- `PostProcessingManager.cs` — 9 per-arena presets (PolarPush through Podium), smooth 1.2s SmoothStep transitions; `MiniGameBase.StartGame()` auto-applies

### Project state at end of session
- Git HEAD: `847a85a`
- Total files: 110 (scripts, scenes, shaders, materials, prefabs, assets, HTML)
- All 12 scenes: ✅ complete
- All 7 mini-game scripts: ✅ complete
- All 8 hero materials: ✅ fully tuned
- All 7 arena material sets: ✅ complete
- VFX: ✅ 9 effects, procedural fallback always active
- Phone controller: ✅ 7 game layouts, full WebSocket protocol
- Post-FX: ✅ 9 arena presets, smooth transitions

### Still remaining (low priority)
1. Add `protected override ArenaPostFX` + `LayoutId` to each mini-game class (2 lines each, 7 files)
2. Texture assets — normal maps, foam textures for OceanWater shader
3. 2D portrait sprites — 512px hero cards for phone/TV UI
4. Audio clip assignments in AudioManager inspector
5. Playtest + balance pass (scoring curves, wave amplitude, damage values)

---

## Session: Unity Setup + Launch Debugging (~06:00–12:34 AM)
**Commits:** `6738c14`, `4809619`, `1f90169`, `ae64e5d`, `3d1ff95`, `263dab4`, `673faf6`, `f3a973c`, `a52d5d7`, `5e7858e`, `b51e8aa`

### What happened
User is new to Unity and tried to launch the game for the first time. Walked through full setup:
- Created LAUNCH_GUIDE.md and MomentsSceneSetup.cs (auto-registers 12 scenes)
- Discovered all 168 .meta files were missing — generated them with correct fileFormatVersion: 2
- ProjectVersion.txt was missing — added for Unity 6000.0.7f1 (user's installed version)
- All scene files had placeholder GUIDs (e.g. "bootstrap-controller-guid") — replaced with real MD5 GUIDs
- User was opening wrong project folder (Unity's demo project, not moments-game/UnityProject/)
- Correct path: D:\NEW unity\workspace\moments-game\UnityProject
- Multiple compile errors fixed: duplicate methods, missing usings, wrong types, broken shader includes

### Compile errors fixed (all resolved)
- TankBattleGame.cs: extra `}` brace + missing `using System.Collections` + `firePressed` → `actionPressed`
- AudioManager.cs: duplicate OnCountdown/OnGameStart/OnWin methods
- BootstrapController.cs: duplicate MainThreadFlusher class
- PostProcessingManager.cs: missing URP + Core assembly references in asmdef
- VFXBuilder.cs: particle module not in manifest
- ColorClashGame.cs: Color.Distance() doesn't exist → manual sqrt
- MiniGameBase.cs: ControllerLayout enum used with ?? string operator
- AttractSceneController.cs: await in non-async IEnumerator
- PolarPushGame.cs: dashPressed → actionPressed
- QRCodeDisplay.cs: int→byte cast issues in GFMul
- All 7 shaders: ShadowCasterPass.hlsl removed in URP 17 → inline shadow functions

### Current state (game running but pink screen)
- ✅ Scripts compile with 0 errors
- ✅ Bootstrap scene opens, DontDestroyOnLoad systems start
- ✅ URP asset created and assigned
- ❌ Screen still pink — custom shaders incompatible with renderer data setup
- ❌ Attract scene not loading (Addressables not configured — using BootstrapSceneLoader.cs workaround)
- Materials converted to URP Lit shader (933532a4fcc9baf4fa0491de14d08ed7) but user needs to update local files
- User needs to add BootstrapSceneLoader component to Bootstrap Root GameObject

### Next steps for user
1. Add BootstrapSceneLoader.cs to Bootstrap Root in Inspector
2. Update all .mat files on PC (replace custom shader guids with 933532a4fcc9baf4fa0491de14d08ed7)
3. Press Play from Bootstrap.unity → should see Attract scene

## Last Daily Summary
2026-03-25T12:34:00+08:00

---

## Session: GitHub + URP Pink Screen Fix (~12:50–22:44 PM)
**Commits:** `df0cf0a`, `6680493`, `73d2d40`, `f1e5c88`

### GitHub setup
- Created GitHub repo: https://github.com/Noor-agent/moments-game
- User's GitHub username: Noor-agent, email: abasheeri12@gmail.com
- PAT used (temporary): REDACTED_TOKEN (likely short-lived)
- User can now `git pull` from `D:\NEW unity\workspace\moments-game`

### Bugs fixed this session
- `MomentsURPFix.cs` — removed URP assembly reference causing CS0234 error
- `Moments.Editor.asmdef` — simplified to not reference URP runtime types
- All placeholder GUIDs in ALL 12 scene files replaced with real MD5 GUIDs
  - e.g. `attract-controller-guid` → real hash, `textmeshpro-ugui-guid` → `f4688fdb7df04437aeb4b961a9e85161`
  - Material placeholder guids fixed too (ocean-water-mat-guid, desert-sand-mat-guid, etc.)
- Art scripts moved from `Scripts/Art/` → `Scripts/Core/` so they share `Moments.Runtime` assembly
  - VFXBuilder.cs, CharacterArtBuilder.cs, ProceduralMeshLibrary.cs, ArtBootstrap.cs
- Created `Assets/Settings/MomentsURP.asset` with stable GUID `15ab25be13d63371e6abd3ba70633579`
- `ProjectSettings/GraphicsSettings.asset` — fixed placeholder `urp-pipeline-asset-guid` → real GUID

### Current state (end of session)
- ✅ Scripts compile with 0 errors
- ✅ Bootstrap scene opens correctly, all systems in Hierarchy
- ✅ Pink screen fixed for AttractBackground quad (was "Missing Material" → Default-Material assigned manually)
- ✅ Clear Flags changed to Solid Color in Attract scene camera
- ❌ URP pipeline asset still showing "None" in Graphics Settings — MomentsURP.asset not recognized by Unity's picker
- ❌ No URP asset auto-assigned yet — need user to manually create one in Unity
- ❌ Attract scene canvas content not visible yet

### Active blocker: URP asset not loading
- Created `Assets/Settings/MomentsURP.asset` but Unity doesn't show it in the render pipeline picker
- The YAML format may be incorrect for Unity 6 URP asset serialization
- User needs to: in Project panel → Assets/Settings → right-click → Create → Rendering → URP Universal Renderer Data, then Create → URP Asset
- Alternative: Tools → Moments → Fix URP Renderer menu item (opens Graphics Settings)

### Key technical findings
- Unity 6 doesn't accept hand-written URP asset YAML — must be created inside Unity editor
- Pink screen has two sources: (1) missing material on mesh, (2) no URP pipeline asset assigned
- AttractBackground object had 2 material slots, both broken — fixed manually by assigning Default-Material
- `Moments.Art.asmdef` caused cross-assembly visibility issues — resolved by merging into Core

### Next steps
1. User must create URP asset inside Unity: Assets/Settings → right-click → Create → Rendering → Universal Render Pipeline Asset
2. Assign it in Project Settings → Graphics → Default Render Pipeline
3. Once URP assigned → press Play from Bootstrap.unity
4. Then address Attract scene canvas visibility (AttractSceneController wiring)
